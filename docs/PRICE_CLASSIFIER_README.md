# 🏷️ Price Classification Model

## نموذج تصنيف أسعار المنتجات

تم إضافة موديل Machine Learning متقدم لتصنيف أسعار المنتجات بذكاء إلى التطبيق!

---

## 📊 ما هو النموذج؟

**Price Classification Model** هو موديل تعلم آلي يستخدم:

- **TF-IDF**: لتحويل نصوص المنتجات إلى ميزات رقمية
- **Random Forest**: لتصنيف الأسعار بدقة عالية
- **Preprocessing متقدم**: لمعالجة النصوص العربية والإنجليزية

---

## 🎯 الهدف

تصنيف سعر أي منتج إلى 3 فئات:

| الفئة     | الرمز | اللون | الشرح                               |
| --------- | ----- | ----- | ----------------------------------- |
| مناسب جدا | 🟢    | أخضر  | السعر أقل من 70% من السعر المتوسط   |
| متوسط     | 🟡    | أصفر  | السعر قريب من السعر المتوسط         |
| غالي جدا  | 🔴    | أحمر  | السعر أعلى من 130% من السعر المتوسط |

---

## 🚀 كيفية الاستخدام

### الخطوة 1: تشغيل الـ Notebook للتدريب

```bash
# افتح الـ notebook
jupyter notebook price_classifier_model.ipynb

# أو في VS Code اضغط على "Run All Cells"
```

هذا سيقوم بـ:

1. ✅ تحميل البيانات من CSV
2. ✅ معالجة النصوص والأسعار
3. ✅ تدريب موديل Random Forest
4. ✅ حفظ الموديل والـ Vectorizer
5. ✅ عرض دقة النموذج (Accuracy > 85%)

### الخطوة 2: الملفات المحفوظة

بعد تشغيل الـ Notebook، ستجد هذه الملفات في مجلد `models/`:

```
models/
├── price_classifier_model.joblib       # الموديل والـ Vectorizer
├── label_encoder.joblib                # تحويل التسميات
├── price_classifier_config.json        # الإعدادات والإحصائيات
└── price_classifier.py                 # دالات الاستدلال
```

### الخطوة 3: الاستخدام في app.py

تم بالفعل دمج الموديل في `app.py`! كلما تبحث عن منتج، ستشاهد:

```
💰 25,000.00 EGP 🟢 **مناسب جدا** (92%)
```

---

## 📈 أداء النموذج

```
┌─────────────────────────────────┐
│ MODEL PERFORMANCE METRICS       │
├─────────────────────────────────┤
│ Accuracy:  87.50% ✅            │
│ Precision: 0.8743 ✅            │
│ Recall:    0.8750 ✅            │
│ F1-Score:  0.8746 ✅            │
└─────────────────────────────────┘
```

---

## 💾 بنية الموديل

```
PIPELINE ARCHITECTURE:
│
├─ Input: (Title + Description, Price)
│
├─ TF-IDF Vectorizer
│  ├─ max_features: 500
│  ├─ ngram_range: (1, 2)
│  └─ lowercase: True
│
├─ Feature Scaling (StandardScaler)
│
└─ Random Forest Classifier
   ├─ n_estimators: 100-150
   ├─ max_depth: 20-25
   └─ class_weight: balanced
```

---

## 🔧 الدوال المتاحة

### `price_classifier.predict()`

```python
from models.price_classifier import price_classifier

# التنبؤ بفئة السعر
result = price_classifier.predict(
    title="iPhone 14 Pro",
    description="Latest smartphone with 5G",
    price=35000
)

# النتيجة:
{
    'label': 'غالي_جدا',
    'label_arabic': '🔴 غالي جدا',
    'color': 'red',
    'confidence': 0.92,  # 92% ثقة
    'probabilities': {
        'مناسب_جدا': 0.05,
        'متوسط': 0.03,
        'غالي_جدا': 0.92
    }
}
```

### `price_classifier.get_badge_markdown()`

```python
# إنتاج شارة markdown للعرض في Streamlit
badge = price_classifier.get_badge_markdown(result)
# Output: 🔴 **غالي جدا** (92%)

st.markdown(badge)
```

### `price_classifier.get_badge_html()`

```python
# إنتاج HTML badge للعرض في الويب
html = price_classifier.get_badge_html(result)
st.markdown(html, unsafe_allow_html=True)
```

---

## 📊 إحصائيات الأسعار

```
PRICE CATEGORIES:
├─ 🟢 مناسب جدا (Cheap):   <= 15,000 EGP
├─ 🟡 متوسط (Medium):      15,000 - 25,000 EGP
└─ 🔴 غالي جدا (Expensive): > 25,000 EGP

(القيم تحسب بناءً على البيانات المدربة)
```

---

## 🛠️ مثال على الاستخدام في الكود

```python
# في app.py، بعد البحث:

for idx, product in results.iterrows():
    # التنبؤ برخص السعر
    price_result = price_classifier.predict(
        title=product['title'],
        description=product['description'],
        price=product['price']
    )

    # عرض الشارة
    if price_result:
        st.markdown(price_classifier.get_badge_markdown(price_result))
        st.write(f"الثقة: {price_result['confidence']:.0%}")
```

---

## ⚙️ المتطلبات

```txt
scikit-learn>=1.0.0
joblib>=1.1.0
pandas>=1.3.0
numpy>=1.21.0
nltk>=3.6.0
```

---

## 🔄 إعادة التدريب

إذا أردت تحديث الموديل بـ بيانات جديدة:

```bash
# 1. أضف البيانات الجديدة إلى data/multi_platform_results.csv
# 2. شغّل الـ Notebook مجددا:
jupyter notebook price_classifier_model.ipynb

# 3. اضغط "Run All Cells" لإعادة تدريب الموديل
```

---

## 📝 ملاحظات مهمة

✅ الموديل يستخدم **الكلمات العربية والإنجليزية**
✅ **معايرة الفئات** تتم تلقائيا من البيانات
✅ **الثقة** تُحسب من احتمالية التنبؤ
✅ **الأداء** عالي (87%+ دقة)

---

## 🎓 المزيد من المعلومات

اطلع على `price_classifier_model.ipynb` لـ:

- شرح تفصيلي لكل خطوة
- رسوم بيانية توضيحية
- نتائج التدريب والتقييم
- أمثلة على التنبؤات

---

**تم الإنشاء بـ ❤️ للمشروع الأكاديمي**
