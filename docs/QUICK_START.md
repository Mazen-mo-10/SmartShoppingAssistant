# 🚀 Quick Start Guide - نموذج تصنيف الأسعار

## خطوات سريعة للبدء

### 1️⃣ **تثبيت المكتبات المطلوبة**

```bash
pip install -r requirements.txt
```

أو إذا كانت مثبتة مسبقا:

```bash
pip install scikit-learn joblib matplotlib seaborn
```

---

### 2️⃣ **تشغيل Notebook لتدريب الموديل**

#### الطريقة الأولى: Jupyter في Terminal

```bash
cd e:\My_Perfect-Projects\shoppingAssistent
jupyter notebook price_classifier_model.ipynb
```

#### الطريقة الثانية: في VS Code

1. افتح الملف `price_classifier_model.ipynb`
2. في الأعلى اضغط **"Select Kernel"** واختر Python
3. اضغط **"Run All"** أو اضغط Ctrl+Shift+Enter

---

### 3️⃣ **انتظر انتهاء التدريب** ⏳

الـ Notebook سيقوم بـ:

- ✅ تحميل البيانات
- ✅ معالجة النصوص
- ✅ تدريب Random Forest (قد يستغرق 1-2 دقيقة)
- ✅ حفظ الموديل تلقائيا في مجلد `models/`

ستشاهد إخراج مشابه:

```
✅ All libraries imported successfully!
✅ Data loaded successfully!
🧹 Preprocessing text data...
✅ Sample of cleaned text:
🏷️ Creating price categories...
📊 Label Distribution:
🚀 Building Pipeline with Random Forest...
🔍 Hyperparameter tuning with RandomizedSearchCV...
📊 Evaluating model on test set...
...
🎉 PRICE CLASSIFICATION MODEL SUMMARY
```

---

### 4️⃣ **تشغيل التطبيق**

```bash
streamlit run app.py
```

الآن عندما تبحث عن منتج ستشاهد شارة ملونة تظهر السعر:

```
💰 25,000.00 EGP 🟢 **مناسب جدا** (92%)
```

---

## 📁 ملفات النموذج المُنشأة

بعد تشغيل الـ Notebook ستجد:

```
models/
├── price_classifier_model.joblib        (180 MB تقريبا)
├── label_encoder.joblib                 (1 KB)
├── price_classifier_config.json         (3 KB)
└── price_classifier.py                  ✨ (مكتب مسبقا)
```

---

## 🎯 ماذا يحدث داخل التطبيق؟

```
المستخدم يبحث عن منتج
        ↓
البحث والحصول على النتائج
        ↓
لكل منتج:
  ├─ استخراج العنوان والسعر
  ├─ معالجة النص بـ TF-IDF
  ├─ التنبؤ بـ Random Forest
  └─ عرض الشارة الملونة (🟢🟡🔴)
        ↓
عرض النتائج مع التصنيفات اللونية
```

---

## ✅ كيف تعرف أن كل شيء يعمل؟

1. **في الـ Notebook**: يجب أن ترى `Accuracy > 85%`
2. **في app.py**: يجب أن تشاهد شارات ملونة بجانب الأسعار
3. **في console**: لا توجد رسائل خطأ حمراء

---

## 🔴 الأخطاء الشائعة وحلولها

### ❌ `ModuleNotFoundError: No module named 'sklearn'`

**الحل:**

```bash
pip install scikit-learn
```

### ❌ `FileNotFoundError: models/price_classifier_model.joblib`

**الحل:**

- تأكد من تشغيل الـ Notebook أولا
- تأكد من وجود البيانات في `data/multi_platform_results.csv`

### ❌ `ValueError: X_train has a different number of features`

**الحل:**

- شغّل الـ Notebook كاملا من جديد بـ "Run All"
- تجاهل الأخطاء البسيطة في الخطوات الأولى

---

## 📊 الأداء المتوقع

| المترية       | القيمة    |
| ------------- | --------- |
| **Accuracy**  | 85-95%    |
| **Precision** | 0.85-0.95 |
| **Recall**    | 0.85-0.95 |
| **F1-Score**  | 0.85-0.95 |

---

## 🎨 الألوان والرموز

| التصنيف   | اللون   | الرمز | المعنى        |
| --------- | ------- | ----- | ------------- |
| مناسب جدا | أخضر 🟢 | ✅    | سعر منخفض جدا |
| متوسط     | أصفر 🟡 | ⚠️    | سعر عادي      |
| غالي جدا  | أحمر 🔴 | ❌    | سعر مرتفع جدا |

---

## 📝 ملاحظات مهمة

- ⏱️ أول تشغيل قد يستغرق 2-3 دقائق
- 💾 حجم الموديل حوالي 180 MB
- 🌍 يدعم النصوص العربية والإنجليزية
- 🔄 يمكنك تحديث الموديل بإعادة تشغيل الـ Notebook

---

## ❓ أسئلة شائعة

**س: هل يمكنني استخدام الموديل بدون تشغيل الـ Notebook؟**

- لا، يجب تشغيل الـ Notebook مرة واحدة لتدريب الموديل

**س: كم مرة أحتاج لتشغيل الـ Notebook؟**

- مرة واحدة للتدريب الأولي
- أعد التشغيل إذا أضفت بيانات جديدة كبيرة

**س: هل الموديل يعمل بدون إنترنت؟**

- نعم! بعد التدريب يعمل 100% بدون إنترنت

---

## 🆘 تحتاج للمساعدة؟

1. اقرأ `PRICE_CLASSIFIER_README.md` للمزيد من التفاصيل
2. افتح الـ Notebook وشغّل كل خلية على حدة
3. تحقق من console للأخطاء التفصيلية

---

**🎉 تم! الآن لديك موديل ML متكامل يعمل مع التطبيق!**
